SELECT FIRST_NAME, DEPARTMENT FROM EMPLOYEE
LEFT JOIN DEPARTMENT ON DEPARTMENT.DEPT_NO = EMPLOYEE.DEPT_NO;

SELECT CONTACT_FIRST, PO_NUMBER FROM SALES
LEFT JOIN CUSTOMER ON SALES.CUST_NO = CUSTOMER.CUST_NO;